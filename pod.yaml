apiVersion: v1
kind: Pod
metadata:
  name: my-web-app
  labels:
    app: salma
spec:
  containers:
    - name: my-nginx-app
      image: salomamagdy11/salma2-test1
      imagePullPolicy: IfNotPresent
      ports:
        - containerPort: 80
